version: '3.8'

services:
  lansman:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    restart: unless-stopped
    container_name: lansman-app
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
